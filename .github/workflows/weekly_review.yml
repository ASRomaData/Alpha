name: Weekly Review

on:
  schedule:
    # Ogni lunedì alle 09:00 UTC (11:00 CET estate, 10:00 inverno)
    - cron: '0 9 * * 1'
  workflow_dispatch: {}

jobs:
  weekly-review:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - run: pip install -r requirements.txt
      - run: mkdir -p data visuals

      - name: Restore history
        uses: actions/cache@v4
        with:
          path: data/history.json
          key: asromadata-history-${{ github.run_id }}
          restore-keys: asromadata-history-

      - name: Run weekly review
        env:
          GROQ_API_KEY:       ${{ secrets.GROQ_API_KEY }}
          IG_USER_ID:         ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN:    ${{ secrets.IG_ACCESS_TOKEN }}
          X_API_KEY:          ${{ secrets.X_API_KEY }}
          X_API_SECRET:       ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN:     ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET:    ${{ secrets.X_ACCESS_SECRET }}
          X_BEARER_TOKEN:     ${{ secrets.X_BEARER_TOKEN }}
          BSKY_HANDLE:        ${{ secrets.BSKY_HANDLE }}
          BSKY_PASSWORD:      ${{ secrets.BSKY_PASSWORD }}
          GH_TOKEN:           ${{ secrets.GH_PAT }}
          GH_OWNER:           ${{ github.repository_owner }}
          GH_REPO:            ${{ github.event.repository.name }}
        run: python -m bot.weekly_review

      - name: Commit updated history
        if: success()
        run: |
          git config user.name  "ASRomaData Bot"
          git config user.email "bot@asromadata.com"
          git add data/ || true
          git diff --staged --quiet || git commit -m "bot: weekly review update [skip ci]"
          git push || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
